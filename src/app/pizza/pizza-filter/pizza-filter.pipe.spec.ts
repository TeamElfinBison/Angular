import { PizzaFilterPipe } from './pizza-filter.pipe';

describe('PizzaFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PizzaFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
