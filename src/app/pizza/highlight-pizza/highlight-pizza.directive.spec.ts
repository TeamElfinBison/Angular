import { HighlightPizzaDirective } from './highlight-pizza.directive';

describe('HighlightPizzaDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightPizzaDirective();
    expect(directive).toBeTruthy();
  });
});
