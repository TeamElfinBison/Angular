import { DeliveryStatusPipe } from './delivery-status.pipe';

describe('DeliveryStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new DeliveryStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
